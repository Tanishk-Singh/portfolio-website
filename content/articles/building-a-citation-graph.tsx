export default function BuildingACitationGraphContent() {
  return (
    <>
      <p>Placeholder — replace with the real breakdown.</p>
      <h2>Four decisions that shape the whole graph</h2>
      <p>
        Direction (cited-by vs. citing), stopping rule (depth vs. saturation),
        dedup data structure (hash set vs. normalized DOI table), and how to
        handle dangling edges — each one quietly determines what the
        finished graph can and can't answer.
      </p>
    </>
  );
}
